/**
 * @deprecated Este componente ya no se usa.
 * Fue reemplazado por `SideDrawer` + `BottomIsland`.
 * Se mantiene aquí solo por historial — puede eliminarse sin riesgo.
 */
export const HamburgerMenu = () => null;
